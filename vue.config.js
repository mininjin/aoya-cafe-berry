/** @type {import('@vue/cli-service').ProjectOptions} */

const config = {
  publicPath: "./",
  pages: {
    index: {
      title: "café Berry",
      entry: "src/main.ts",
    },
  },
};

module.exports = config;
